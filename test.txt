fatorial (int fat) : int
    if (lexp):
        print (fat);
        return calc;
    else:
        return 1;
    end
end

mostrarMedia (int v1, int v2) : void
    var:
        const a = 10;
        x : int;
        y : int;
    x = 5 + 5 * 3;
    print ("Resultado: ", media(v1, v2));
end

media (int n1, int n2) : float
    var:
         m : float;
    m = (n1 + n2) / 2;
    return m;
end 

main:
    var:
        const cout = 10.0;
        numero : int;
        n1, n2 : int;
        isBoolean: bool;
    
    isBoolean = true;
    isBoolean = false;
    print ("Programa Fatorial. Digite o valor?");
    scanf(numero);
    print(fatorial(numero));
    print ("Programa Media. Digite o valores?");
    scanf(n1, n2);
    print(mostrarMedia(n1, n2));
end